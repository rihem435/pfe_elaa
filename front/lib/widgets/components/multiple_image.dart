import 'package:flutter/material.dart';

class MultipleImage extends StatelessWidget {
const MultipleImage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Container();
  }
}